export enum GameStateType {
    INIT = 0,
    READY = 1,
    PLAYING = 2,
}
export enum FacingType {
    Scale,
    Angle,
}
export enum TypeCharacter {
    Player,
    Enemy,
}
export enum OperationType {
    Plus,
    Multiply,
    Substract,
    Divine
}

